name: test
on: workflow_dispatch

jobs:
  test-action:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: test-step
      uses: ./.github/actions
      with:
        test-input: abc,cde,fgh